The physical address is an exact hardware address of a physical memory cell.
The virtual address is an address of the virtual memory which is an abstraction of physical memory made by an Operating System. 
PAGING is an algorithm for mapping from virtual to physical address (which is made by a MMU).
